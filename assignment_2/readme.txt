il seguente link contiene tutti i pesi necessari per eseguire i plot del modello definito dal docente: https://drive.google.com/drive/u/1/folders/1s1RBWL8LyEaFCgUIWBumEfpLP03hHdts Dopo aver scaricato i file e averli inseriti nella cartella relativa al problema (senza cambiare il nome al file) dovrebbe essere possibile ricreare le heatmap. Se il codice viene eseguito su colab è possibile importare i pesi solo caricandoli nella macchina virtuale, senza inserirli in una cartella, e non tramite il collegamento della cartella di google drive.

Se il codice viene eseguito in locale potrebbe essere necessario cambiare il kernel e installare le librerie necessarie. Se si vuole plottare il modello è necessario installare pydot tramite il comando pip install pydot, tuttavia successivamente non andrà importata.

Purtroppo le immagini dei pesi del problema 2 erano troppo pesanti, di conseguenza il file .zip eccedeva il limite imposto da elearning di 10MB. Per questo motivo ho dovuto eliminare gli output di alcune celle e tutte le heatmap dei pesi del problema 2. le condivido il link del file colab con tutti gli output se vuole visualizzarli senza eseguire il codice https://colab.research.google.com/drive/1NzrCFFaY8JRZQ0iaWSPbXfTSmgSulpw4?usp=sharing
